// 非Web平台的iframe管理实现（空实现）
class IframeManagerImpl {
  // 禁用所有iframe的点击事件（非Web平台无操作）
  static void disableAllIframes() {
    // 非Web平台不需要操作
  }
  
  // 重新启用所有iframe的点击事件（非Web平台无操作）
  static void enableAllIframes() {
    // 非Web平台不需要操作
  }
}